# =====================================================================
# FintechDB v1.0 — pg_hba.conf
# Intentionally insecure for the PgVault project.
# H03 planted here: trust auth from 0.0.0.0/0 for app_legacy
# =====================================================================
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Standard local connections (kept secure)
local   all             postgres                                trust
local   all             all                                     md5

# IPv4 local connections
host    all             postgres        127.0.0.1/32            md5

# H03: trust auth from 0.0.0.0/0 for app_legacy (DANGEROUS)
# A detector should flag any 'trust' rule from 0.0.0.0/0
host    all             app_legacy      0.0.0.0/0               trust

# Other connections require md5
host    all             admin           0.0.0.0/0               md5
host    all             reports_user    0.0.0.0/0               md5
host    all             analyst_user    0.0.0.0/0               md5
host    all             fintech_user    0.0.0.0/0               md5
host    all             all             0.0.0.0/0               md5

# IPv6
host    all             all             ::1/128                 md5
